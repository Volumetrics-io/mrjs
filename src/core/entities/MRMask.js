MRMask.js
// may be useful as an understanding separate from mr-panel and overflow:hidden
// otherwise let users lean entirely on overflow: hidden

// need to solve the double masking bug first before determining if this separate file is even necessary though
